import 'package:kiosk_point_of_sale/core/flutter_flow/flutter_flow_model.dart';

import 'cart_widget.dart' show CartWidget;
import 'package:flutter/material.dart';

class CartModel extends FlutterFlowModel<CartWidget> {
  ///  State fields for stateful widgets in this page.

  // Model for CheckoutItem2 component.
  // late CheckoutItem2Model checkoutItem2Model1;
  // // Model for CheckoutItem2 component.
  // late CheckoutItem2Model checkoutItem2Model2;

  @override
  void initState(BuildContext context) {
    // checkoutItem2Model1 = createModel(context, () => CheckoutItem2Model());
    // checkoutItem2Model2 = createModel(context, () => CheckoutItem2Model());
  }

  @override
  void dispose() {
    // checkoutItem2Model1.dispose();
    // checkoutItem2Model2.dispose();
  }

  /// Action blocks.
  Future back(BuildContext context) async {}
}
